import "./App.css";

function App() {
  const handleSelect = (option) => {
    setSelection(option);
  };
  return <div className="App position-relative container-fluid"></div>;
}

export default App;
